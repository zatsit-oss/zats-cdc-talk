apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: conduktor-data
  name: conduktor-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
